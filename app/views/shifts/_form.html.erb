<%= form_for @shift, url: {action: "update"} do |f| %>
  <% if @shift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shift.errors.count, "error") %> prohibited this shift from being saved:</h2>

      <ul>
      <% @shift.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <hr/>

  <%= render partial: "shared/trailer_checklist",
    locals: {f: f, section_header: "Inside Trailer", section: "inside", checklist_type: "shift"} %>

  <hr/>

  <%= render partial: "shared/trailer_checklist",
    locals: {f: f, section_header: "Outside Trailer", section: "outside", checklist_type: "shift"} %>

  <div class="actions">
    <%= f.submit "Save" %>
  </div>
<% end %>
