<%= form_for(@checklist_config) do |f| %>
  <% if @checklist_config.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@checklist_config.errors.count, "error") %> prohibited this checklist_config from being saved:</h2>

      <ul>
      <% @checklist_config.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
