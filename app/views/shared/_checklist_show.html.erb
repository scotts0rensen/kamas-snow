<h2><%= section_header %></h2>
<ul>
  <% ChecklistConfig.where(checklist_type: checklist_type, section: section).order(:order).each do |config| %>
    <li>
      <span><%= config.name %>:</span>
      <span><%= checklist.send(config.name.to_sym) %></span>
    </li>
  <% end %>
</ul>
